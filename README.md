# OpenAI-ChatGPT
a simple Chat GPT robot

Run the Chat GPT via main.py. 
Runs based on openai API key.

You need your own API key, which can be get from https://platform.openai.com/account/api-keys

After that you need to create your own txt file with the name "OPENAI_API.txt" and place inside the API key.
